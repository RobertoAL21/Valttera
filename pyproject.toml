[project]
name = "house-price-prediction"
version = "0.1.0"
description = "End-to-end Ames house-price prediction API."
requires-python = ">=3.10,<3.13"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
